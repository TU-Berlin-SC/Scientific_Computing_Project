[package]
name = "runner"
version = "0.1.0"
edition = "2021"

[dependencies]
engine = { path = "../engine", features = ["native"] } # native 기능 활성화
serde = { version = "1.0", features = ["derive"] }
serde-wasm-bindgen = "0.6.5"
serde_json = "1.0"
